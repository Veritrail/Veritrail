# Platform-admin SPA (admin.veritrail.io) — plain static files, no build step.
FROM nginx:alpine
COPY admin/nginx/default.conf /etc/nginx/conf.d/default.conf
COPY admin/index.html admin/veritrail-mark.png /usr/share/nginx/html/
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
