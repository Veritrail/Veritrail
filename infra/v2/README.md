# Connector template v2 (recommended)

Published to S3 as `infra/v2/veritrail-stack.yaml`.

Includes current remediation module parameters and SSM runbook alignment. This is the default recommended connector update version in the Veritrail app.

Source of truth in-repo: `infra/cfn/veritrail-stack.yaml` on the release branch that cut v2.
