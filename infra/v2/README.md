# Connector template v2 (recommended)

Published to S3 as `infra/v2/vigil-stack.yaml`.

Includes current remediation module parameters and SSM runbook alignment. This is the default recommended connector update version in the Vigil app.

Source of truth in-repo: `infra/cfn/vigil-stack.yaml` on the release branch that cut v2.
