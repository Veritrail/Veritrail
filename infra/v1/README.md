# Connector template v1

Published to S3 as `infra/v1/veritrail-stack.yaml` (same bucket as `CFN_TEMPLATE_URL`).

Source of truth in-repo: `infra/cfn/veritrail-stack.yaml` at the release tag that cut v1.
