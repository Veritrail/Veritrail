AWSTemplateFormatVersion: "2010-09-09"
Description: >
  Vigil remediation SSM Automation runbooks. Python handlers live in S3
  (infra/<release>/ssm-scripts/*.py); publish with ./scripts/upload-cfn.sh
  before updating this stack.

Parameters:
  AutomationRoleName:
    Type: String
    Default: VigilRemediationAutomationRole
  SsmScriptsBucket:
    Type: String
    Default: amzn-s3-vigil
    Description: S3 bucket hosting Vigil SSM handler scripts (public-read).
  SsmScriptsRelease:
    Type: String
    Default: "2026.06"
    Description: Release folder under infra/ (must match upload-cfn.sh RELEASE).
  EnableSecurityGroupRemediation:
    Type: String
    Default: "No"
    AllowedValues: ["Yes", "No"]
  EnableS3Remediation:
    Type: String
    Default: "No"
    AllowedValues: ["Yes", "No"]
  EnableIamAccessKeyRemediation:
    Type: String
    Default: "No"
    AllowedValues: ["Yes", "No"]
  EnableIamPolicyRemediation:
    Type: String
    Default: "No"
    AllowedValues: ["Yes", "No"]
  EnableSsmParameterRemediation:
    Type: String
    Default: "No"
    AllowedValues: ["Yes", "No"]
  EnableCloudTrailRemediation:
    Type: String
    Default: "No"
    AllowedValues: ["Yes", "No"]

Conditions:
  SecurityGroupModule: !Equals [!Ref EnableSecurityGroupRemediation, "Yes"]
  IamAccessKeyModule: !Equals [!Ref EnableIamAccessKeyRemediation, "Yes"]
  IamPolicyModule: !Equals [!Ref EnableIamPolicyRemediation, "Yes"]
  SsmParameterModule: !Equals [!Ref EnableSsmParameterRemediation, "Yes"]
  S3Module: !Equals [!Ref EnableS3Remediation, "Yes"]
  AnyRemediationModule: !Or
    - !Condition SecurityGroupModule
    - !Condition IamAccessKeyModule
    - !Condition IamPolicyModule
    - !Condition SsmParameterModule
    - !Condition S3Module

Mappings:
  SsmScriptChecksums:
    RevokeSgIngress:
      sha256: adab1144fdcdd2ff52671bb35154780ea8c160c1dbda8e5b511f2b0492ed9aea
    DeactivateAccessKey:
      sha256: 81ffa9e85bc35bccad2c8cebb04fed3c3b22ecc9ca3b95ee149b9a5f95a04ebd
    MigrateSecureString:
      sha256: bc4030538433deffe0d80d64c8de6dfecc7bc4f72ad125290d8a366aaf80d65a
    RemediateExcessPermissions:
      sha256: 6d14a428d6677ca3f88d8a19e46645bec478bae6c9a7581f66e94f1d1e998416

Resources:
  RemediationAutomationRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Ref AutomationRoleName
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ssm.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: VigilRemediationAutomation
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - !If
                - AnyRemediationModule
                - Sid: SsmHandlerScriptsFromS3
                  Effect: Allow
                  Action:
                    - s3:GetObject
                  Resource: !Sub
                    - arn:aws:s3:::${Bucket}/infra/${Release}/ssm-scripts/*
                    - Bucket: !Ref SsmScriptsBucket
                      Release: !Ref SsmScriptsRelease
                - !Ref AWS::NoValue
              # Always on this role — AWS-owned S3 PAB runbook; avoids nested-stack !If not updating.
              - Sid: S3BucketPublicAccessBlock
                Effect: Allow
                Action:
                  - s3:GetBucketPublicAccessBlock
                  - s3:PutBucketPublicAccessBlock
                Resource: arn:aws:s3:::*
              - !If
                - SecurityGroupModule
                - Sid: Ec2SecurityGroupIngress
                  Effect: Allow
                  Action:
                    - ec2:DescribeSecurityGroups
                    - ec2:DescribeSecurityGroupRules
                    - ec2:RevokeSecurityGroupIngress
                  Resource: "*"
                - !Ref AWS::NoValue
              - !If
                - SsmParameterModule
                - Sid: SsmParameterSecureStringMigration
                  Effect: Allow
                  Action:
                    - ssm:GetParameter
                    - ssm:PutParameter
                  Resource: "*"
                - !Ref AWS::NoValue
              - !If
                - IamAccessKeyModule
                - Sid: IamAccessKeyRemediation
                  Effect: Allow
                  Action:
                    - iam:UpdateAccessKey
                    - iam:GetAccessKeyLastUsed
                  Resource: "*"
                - !Ref AWS::NoValue
              - !If
                - IamPolicyModule
                - Sid: IamPolicyRemediation
                  Effect: Allow
                  Action:
                    - iam:GetRole
                    - iam:GetRolePolicy
                    - iam:PutRolePolicy
                    - iam:ListAttachedRolePolicies
                    - iam:DetachRolePolicy
                    - iam:GetPolicy
                  Resource: "*"
                - !Ref AWS::NoValue

  RevokeSecurityGroupIngressDocument:
    Type: AWS::SSM::Document
    Condition: SecurityGroupModule
    Properties:
      Name: Vigil-RevokeSecurityGroupIngressExact
      DocumentType: Automation
      DocumentFormat: YAML
      UpdateMethod: NewVersion
      Attachments:
        - Key: S3FileUrl
          Name: revoke_sg_ingress.py
          Values:
            - !Sub
              - s3://${Bucket}/infra/${Release}/ssm-scripts/revoke_sg_ingress.py
              - Bucket: !Ref SsmScriptsBucket
                Release: !Ref SsmScriptsRelease
      Content:
        schemaVersion: "0.3"
        description: Revoke only the exact public ingress rule from a signed Vigil plan.
        assumeRole: !GetAtt RemediationAutomationRole.Arn
        parameters:
          PlanJson:
            type: String
            description: Signed Vigil remediation plan JSON.
        files:
          revoke_sg_ingress.py:
            checksums:
              sha256: !FindInMap [SsmScriptChecksums, RevokeSgIngress, sha256]
        mainSteps:
          - name: RevokeExactIngress
            action: aws:executeScript
            timeoutSeconds: 300
            inputs:
              Runtime: python3.11
              Handler: revoke_sg_ingress.handler
              Attachment: revoke_sg_ingress.py
              InputPayload:
                PlanJson: "{{ PlanJson }}"

  DeactivateIamAccessKeyDocument:
    Type: AWS::SSM::Document
    Condition: IamAccessKeyModule
    Properties:
      Name: Vigil-DeactivateIamAccessKey
      DocumentType: Automation
      DocumentFormat: YAML
      UpdateMethod: NewVersion
      Attachments:
        - Key: S3FileUrl
          Name: deactivate_access_key.py
          Values:
            - !Sub
              - s3://${Bucket}/infra/${Release}/ssm-scripts/deactivate_access_key.py
              - Bucket: !Ref SsmScriptsBucket
                Release: !Ref SsmScriptsRelease
      Content:
        schemaVersion: "0.3"
        description: Deactivate a reviewed IAM access key (never delete).
        assumeRole: !GetAtt RemediationAutomationRole.Arn
        parameters:
          PlanJson:
            type: String
            description: Signed Vigil remediation plan JSON.
        files:
          deactivate_access_key.py:
            checksums:
              sha256: !FindInMap [SsmScriptChecksums, DeactivateAccessKey, sha256]
        mainSteps:
          - name: DeactivateAccessKey
            action: aws:executeScript
            timeoutSeconds: 300
            inputs:
              Runtime: python3.11
              Handler: deactivate_access_key.handler
              Attachment: deactivate_access_key.py
              InputPayload:
                PlanJson: "{{ PlanJson }}"

  MigrateSsmParameterDocument:
    Type: AWS::SSM::Document
    Condition: SsmParameterModule
    Properties:
      Name: Vigil-MigrateSsmParameterToSecureString
      DocumentType: Automation
      DocumentFormat: YAML
      UpdateMethod: NewVersion
      Attachments:
        - Key: S3FileUrl
          Name: migrate_to_secure_string.py
          Values:
            - !Sub
              - s3://${Bucket}/infra/${Release}/ssm-scripts/migrate_to_secure_string.py
              - Bucket: !Ref SsmScriptsBucket
                Release: !Ref SsmScriptsRelease
      Content:
        schemaVersion: "0.3"
        description: Migrate a reviewed SSM String parameter to SecureString.
        assumeRole: !GetAtt RemediationAutomationRole.Arn
        parameters:
          PlanJson:
            type: String
            description: Signed Vigil remediation plan JSON.
        files:
          migrate_to_secure_string.py:
            checksums:
              sha256: !FindInMap [SsmScriptChecksums, MigrateSecureString, sha256]
        mainSteps:
          - name: MigrateParameter
            action: aws:executeScript
            timeoutSeconds: 300
            inputs:
              Runtime: python3.11
              Handler: migrate_to_secure_string.handler
              Attachment: migrate_to_secure_string.py
              InputPayload:
                PlanJson: "{{ PlanJson }}"

  RemediateIamExcessPermissionsDocument:
    Type: AWS::SSM::Document
    Condition: IamPolicyModule
    Properties:
      Name: Vigil-RemediateIamExcessPermissions
      DocumentType: Automation
      DocumentFormat: YAML
      UpdateMethod: NewVersion
      Attachments:
        - Key: S3FileUrl
          Name: remediate_excess_permissions.py
          Values:
            - !Sub
              - s3://${Bucket}/infra/${Release}/ssm-scripts/remediate_excess_permissions.py
              - Bucket: !Ref SsmScriptsBucket
                Release: !Ref SsmScriptsRelease
      Content:
        schemaVersion: "0.3"
        description: Narrow IAM role policies per a signed Vigil remediation plan.
        assumeRole: !GetAtt RemediationAutomationRole.Arn
        parameters:
          PlanJson:
            type: String
            description: Signed Vigil remediation plan JSON.
        files:
          remediate_excess_permissions.py:
            checksums:
              sha256: !FindInMap [SsmScriptChecksums, RemediateExcessPermissions, sha256]
        mainSteps:
          - name: RemediateIamPolicy
            action: aws:executeScript
            timeoutSeconds: 300
            inputs:
              Runtime: python3.11
              Handler: remediate_excess_permissions.handler
              Attachment: remediate_excess_permissions.py
              InputPayload:
                PlanJson: "{{ PlanJson }}"

Outputs:
  AutomationRoleArn:
    Value: !GetAtt RemediationAutomationRole.Arn
  RemediationRoleArn:
    Value: !GetAtt RemediationAutomationRole.Arn
  SsmScriptsPrefix:
    Description: S3 prefix for handler scripts (upload before stack update).
    Value: !Sub infra/${SsmScriptsRelease}/ssm-scripts/
  SecurityGroupDocumentName:
    Value: Vigil-RevokeSecurityGroupIngressExact
  IamAccessKeyDocumentName:
    Value: Vigil-DeactivateIamAccessKey
  SsmParameterDocumentName:
    Value: Vigil-MigrateSsmParameterToSecureString
  IamPolicyDocumentName:
    Value: Vigil-RemediateIamExcessPermissions
