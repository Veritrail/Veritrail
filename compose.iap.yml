# IAP edge gate — use with --profile iap when IAP_ENABLED=true in .env
#   docker compose -f compose.yml -f compose.prod.yml -f compose.iap.yml --profile prod --profile iap up -d

services:
  nginx:
    depends_on:
      - oauth2-proxy
